# PostgreSQL Configuration (Production Ready)
DATABASE_URL=postgresql://neondb_owner:npg_TGltjw2AmSK7@ep-red-night-a2mpoekf-pooler.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Alternative PostgreSQL (for local fallback)
#POSTGRES_HOST=localhost
#POSTGRES_PORT=5432  
POSTGRES_DB=rag_db
POSTGRES_USER=rag_user
POSTGRES_PASSWORD=rag_password

# Neo4j Configuration (Production Ready)
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=ifsDTA*******************SGoeKVy0

# Redis Configuration (Caching)
REDIS_URL=redis://redis:6379/0
REDIS_MAX_MEMORY=256mb
REDIS_EVICTION_POLICY=allkeys-lru

# Misc
METRICS_PORT=9090

# Neo4j Timeout & Retry Settings (Agent OS v2.0)
NEO4J_TIMEOUT=300        # Timeout in seconds (5 minutes default)
NEO4J_MAX_RETRY=3        # Maximum retry attempts
NEO4J_RETRY_DELAY=5      # Delay between retries in seconds

# PostgreSQL UUID Extension (required for Neon)
# Neon PostgreSQL cloud includes uuid-ossp by default

# LLM Provider Configuration  
LLM_PROVIDER=openai
LLM_CHOICE=gpt-4.1-mini
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=sk-proj-egi1*****************************************************************************Ib17QeNlYiWPFOCe9hGBz3lZ69QehtIA

# OpenAI Timeout & Retry Settings (Agent OS v2.0)
OPENAI_TIMEOUT=120           # Timeout in seconds (2 minutes default)
OPENAI_MAX_RETRY=3           # Maximum retry attempts
OPENAI_RETRY_DELAY=5         # Delay between retries in seconds

# Embedding Configuration
EMBEDDING_PROVIDER=openai
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_BASE_URL=https://api.openai.com/v1
EMBEDDING_API_KEY=sk-your_actual_openai_api_key_here
VECTOR_DIMENSION=1536

# Medical Domain Configuration
INGESTION_LLM_CHOICE=gpt-4.1-nano
MEDICAL_ENTITY_EXTRACTION=true


# Medical Content Chunking (optimized for fisioterapia)
CHUNK_SIZE=600
CHUNK_OVERLAP=100
MAX_CHUNK_SIZE=1200

# Medical Vector Search Configuration
VECTOR_DIMENSION=1536  # OpenAI text-embedding-3-small
MAX_SEARCH_RESULTS=15  # More results for comprehensive medical coverage

# Medical Session Configuration
SESSION_TIMEOUT_MINUTES=120  # Longer sessions for educational use
MAX_MESSAGES_PER_SESSION=150

# Rate Limiting for Educational Use
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW_SECONDS=60

# Medical File Processing
MAX_FILE_SIZE_MB=20  # Larger files for medical documentation
ALLOWED_FILE_EXTENSIONS=.md,.txt,.pdf

# Medical Quiz Configuration
QUIZ_DEFAULT_QUESTIONS=5
QUIZ_MAX_QUESTIONS=10
QUIZ_DEFAULT_DIFFICULTY=medium
QUIZ_LANGUAGE=italian

# Medical Entity Extraction
EXTRACT_ANATOMICAL=true
EXTRACT_PATHOLOGICAL=true
EXTRACT_TREATMENTS=true
EXTRACT_DEVICES=true

# Debug Configuration
DEBUG_MODE=true
ENABLE_PROFILING=false
ENABLE_MEDICAL_LOGGING=true
